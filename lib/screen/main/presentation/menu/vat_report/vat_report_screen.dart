import 'package:flutter/material.dart';

class VatReportScreen extends StatelessWidget {
  const VatReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Vat Report Screen'),
      ),
    );
  }
}
